---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import landing from '../data/landing.json';
import LeaderboardTable from '../components/LeaderboardTable.jsx';

const GITHUB_REPO = 'https://github.com/aira-race/virtual-robot-race';

const steps = [
  {
    number: '01',
    title: 'Fork & Clone the Repository',
    color: 'sky',
    description: 'Fork the repo to your GitHub account, then clone it to your PC in one click using GitHub Desktop — no terminal needed.',
    items: [
      { type: 'note', content: 'GitHub Desktop must be installed before this step.' },
      { type: 'link', label: 'Download GitHub Desktop →', href: 'https://desktop.github.com/' },
      { type: 'video', src: '/fork-demo.mp4' },
      { type: 'text', content: '① Click the Fork button in the top-right corner of the repository page.' },
      { type: 'link', label: 'Open virtual-robot-race on GitHub →', href: GITHUB_REPO },
      { type: 'text', content: '② After forking, click Code → Open with GitHub Desktop. GitHub Desktop will ask where to save the folder — choose somewhere easy to find.' },
      { type: 'note', content: 'Prefer the terminal? Clone manually with the command below.' },
      { type: 'code', lang: 'bash', content: 'git clone https://github.com/YOUR_NAME/virtual-robot-race.git\ncd virtual-robot-race' },
    ],
  },
  {
    number: '02',
    title: 'Set Up Your Environment',
    color: 'violet',
    description: 'Run the setup script to install all Python dependencies automatically.',
    items: [
      { type: 'video', src: '/setup-env.mp4' },
      { type: 'text', content: 'On Windows, open the cloned folder in Explorer and double-click setup_env.bat.' },
      { type: 'code', lang: 'cmd', content: 'setup_env.bat' },
      { type: 'text', content: 'Or manually with pip:' },
      { type: 'code', lang: 'bash', content: 'pip install -r requirements.txt' },
      { type: 'note', content: 'Python 3.12 or later is recommended (3.13 also works).' },
    ],
  },
  {
    number: '03',
    title: 'Set Your Race Name',
    color: 'orange',
    description: 'Launch the simulator from VSCode — a setup window opens where you can enter your race name.',
    items: [
      { type: 'text', content: 'Open the cloned folder in VSCode, then run start.bat from the integrated terminal:' },
      { type: 'code', lang: 'bat', content: 'start.bat' },
      { type: 'video', src: '/vscode-launch.mp4' },
      { type: 'text', content: 'The aira launcher opens. Enter your name in the Name field, then click START.' },
      { type: 'image', src: '/gui-launcher.png', alt: 'aira launcher — Name field' },
      { type: 'note', content: 'Your display name must match the Name field exactly. Set it in your dashboard before racing.' },
      { type: 'link', label: 'Set your display name in Dashboard →', href: '/dashboard' },
      { type: 'text', content: 'Once your name is set, click START — Unity launches automatically and the race begins.' },
    ],
  },
];

const colorMap: Record<string, { badge: string; border: string; num: string; link: string; langBadge: string }> = {
  sky:     { badge: 'bg-sky-100 text-sky-700',       border: 'border-sky-200',     num: 'text-sky-400',     link: 'text-sky-600 hover:text-sky-800',     langBadge: 'bg-sky-900/60 text-sky-300' },
  violet:  { badge: 'bg-violet-100 text-violet-700', border: 'border-violet-200',  num: 'text-violet-400',  link: 'text-violet-600 hover:text-violet-800', langBadge: 'bg-violet-900/60 text-violet-300' },
  orange:  { badge: 'bg-orange-100 text-orange-700', border: 'border-orange-200',  num: 'text-orange-400',  link: 'text-orange-600 hover:text-orange-800', langBadge: 'bg-orange-900/60 text-orange-300' },
  emerald: { badge: 'bg-emerald-100 text-emerald-700', border: 'border-emerald-200', num: 'text-emerald-400', link: 'text-emerald-600 hover:text-emerald-800', langBadge: 'bg-emerald-900/60 text-emerald-300' },
};
---

<Layout
  title="Getting Started | aira"
  description="Step-by-step guide to set up aira Beta 1.7 and run your first virtual robot race."
  canonicalUrl={landing.site.url + '/getting-started'}
  ogImage={landing.site.ogImage}
  siteName={landing.site.name}
  lang={landing.site.lang}
>
  <Header />
  <main class="bg-arena-bg min-h-screen">

    <!-- Page hero -->
    <div class="relative pt-24 pb-16 px-4 sm:px-6 border-b border-arena-border overflow-hidden">
      <div
        class="absolute inset-0 pointer-events-none"
        style="background-image: linear-gradient(to right, #e4e4e7 1px, transparent 1px), linear-gradient(to bottom, #e4e4e7 1px, transparent 1px); background-size: 48px 48px; opacity: 0.4;"
        aria-hidden="true"
      ></div>
      <div class="relative max-w-3xl mx-auto text-center">
        <span class="inline-block text-xs font-semibold tracking-widest text-sky-600 uppercase mb-4">Tutorial</span>
        <h1 class="text-4xl sm:text-5xl font-light tracking-tight text-arena-ink">
          Getting Started with<br/><span class="font-semibold">aira Beta 1.7</span>
        </h1>
        <p class="mt-6 text-lg text-arena-muted max-w-xl mx-auto">
          Set up your environment, run the simulation, and submit your first result to the leaderboard — no prior AI experience needed.
        </p>
        <div class="mt-8 flex flex-col sm:flex-row items-center justify-center gap-3">
          <a
            href={GITHUB_REPO}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 px-6 py-3 text-sm font-semibold text-white bg-arena-solid rounded-full hover:bg-zinc-700 transition-colors"
          >
            <svg class="h-4 w-4" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path fill-rule="evenodd" d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" clip-rule="evenodd" />
            </svg>
            Fork on GitHub
          </a>
          <a
            href="#step-01"
            class="inline-flex items-center gap-2 px-6 py-3 text-sm font-semibold text-arena-ink border border-arena-border rounded-full hover:border-zinc-400 transition-colors"
          >
            Start the tutorial ↓
          </a>
        </div>
      </div>
    </div>

    <!-- Steps -->
    <div class="max-w-3xl mx-auto px-4 sm:px-6 py-16 space-y-12">
      {steps.map((step) => {
        const c = colorMap[step.color] ?? colorMap.sky;
        return (
          <div id={`step-${step.number}`} class={`rounded-2xl border ${c.border} bg-white p-8 shadow-sm`}>
            <!-- Step header -->
            <div class="flex items-start gap-4 mb-6">
              <span class={`font-mono text-4xl font-bold leading-none ${c.num} opacity-40 select-none`}>{step.number}</span>
              <div>
                <h2 class="text-xl font-semibold text-arena-ink">{step.title}</h2>
                <p class="mt-1 text-sm text-arena-muted">{step.description}</p>
              </div>
            </div>

            <!-- Step content -->
            <div class="space-y-4 ml-14">
              {step.items.map((item) => {
                if (item.type === 'video') {
                  return (
                    <video
                      src={item.src}
                      autoplay
                      loop
                      muted
                      playsinline
                      class="w-full rounded-xl border border-zinc-200 shadow-sm"
                    />
                  );
                }
                if (item.type === 'text') {
                  return <p class="text-sm text-arena-muted">{item.content}</p>;
                }
                if (item.type === 'code') {
                  return (
                    <div class="relative group rounded-xl overflow-hidden border border-zinc-800">
                      <!-- Top bar: lang label + copy button -->
                      <div class="flex items-center justify-between px-4 py-2 bg-zinc-900 border-b border-zinc-800">
                        <span class={`text-xs font-mono font-semibold px-2 py-0.5 rounded ${c.langBadge}`}>
                          {item.lang}
                        </span>
                        <button
                          class="copy-btn flex items-center gap-1.5 text-xs text-zinc-400 hover:text-white transition-colors px-2 py-1 rounded hover:bg-zinc-700"
                          data-code={item.content}
                          aria-label="Copy code"
                        >
                          <svg class="copy-icon h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />
                          </svg>
                          <svg class="check-icon h-3.5 w-3.5 hidden text-emerald-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                          </svg>
                          <span class="copy-label">Copy</span>
                        </button>
                      </div>
                      <!-- Code content -->
                      <pre class="bg-zinc-950 text-zinc-100 px-5 py-4 text-xs font-mono overflow-x-auto whitespace-pre leading-relaxed m-0">{item.content}</pre>
                    </div>
                  );
                }
                if (item.type === 'image') {
                  return (
                    <img
                      src={item.src}
                      alt={item.alt}
                      class="w-full max-w-sm rounded-xl border border-zinc-200 shadow-sm"
                    />
                  );
                }
                if (item.type === 'note') {
                  return (
                    <div class="flex items-start gap-2 text-xs text-amber-700 bg-amber-50 border border-amber-200 rounded-lg px-4 py-3">
                      <span class="mt-0.5 shrink-0">⚠</span>
                      <span>{item.content}</span>
                    </div>
                  );
                }
                if (item.type === 'link') {
                  return (
                    <a
                      href={item.href}
                      target={item.href?.startsWith('http') ? '_blank' : undefined}
                      rel={item.href?.startsWith('http') ? 'noopener noreferrer' : undefined}
                      class={`inline-flex items-center text-sm font-medium underline underline-offset-4 ${c.link} transition-colors`}
                    >
                      {item.label}
                    </a>
                  );
                }
                return null;
              })}
            </div>
          </div>
        );
      })}
    </div>

    <!-- Step 04: Challenge the Leaderboard -->
    <div class="max-w-3xl mx-auto px-4 sm:px-6 pb-12">
      <div class="rounded-2xl border border-emerald-200 bg-white p-8 shadow-sm">
        <div class="flex items-start gap-4 mb-6">
          <span class="font-mono text-4xl font-bold leading-none text-emerald-400 opacity-40 select-none">04</span>
          <div>
            <h2 class="text-xl font-semibold text-arena-ink">Challenge the Leaderboard</h2>
            <p class="mt-1 text-sm text-arena-muted">Hit Submit in the GUI — your time is posted instantly. See how you stack up.</p>
          </div>
        </div>
        <div class="space-y-4 ml-14">
          <p class="text-sm text-arena-muted">In the aira launcher, click <strong>Submit</strong> after your run to register your result.</p>
          <video
            src="/submit-demo.mp4"
            autoplay
            loop
            muted
            playsinline
            class="w-full rounded-xl border border-zinc-200 shadow-sm"
          />
          <div class="pt-4 border-t border-zinc-100">
            <p class="text-sm font-medium text-arena-ink mb-4">Tutorial Leaderboard — see who's already racing</p>
            <LeaderboardTable client:load />
          </div>
        </div>
      </div>
    </div>

    <!-- What's Next -->
    <div class="border-t border-arena-border">
      <div class="max-w-3xl mx-auto px-4 sm:px-6 py-16">
        <div class="mb-10 text-center">
          <span class="inline-block text-xs font-semibold tracking-widest text-sky-600 uppercase mb-3">What's Next?</span>
          <h2 class="text-2xl font-light tracking-tight text-arena-ink">You're up and running — what do you want to do?</h2>
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
          <!-- Learn -->
          <a
            href="https://github.com/aira-race/virtual-robot-race/blob/main/docs/README.md"
            target="_blank"
            rel="noopener noreferrer"
            class="group flex flex-col items-center text-center gap-3 rounded-2xl border border-sky-200 bg-white p-6 shadow-sm hover:border-sky-400 hover:shadow-md transition-all"
          >
            <span class="text-3xl">📖</span>
            <div>
              <p class="font-semibold text-arena-ink">Learn More</p>
              <p class="mt-1 text-xs text-arena-muted">Read the docs and lessons in your language</p>
            </div>
            <span class="text-xs text-sky-600 group-hover:text-sky-800 font-medium mt-auto">docs/README.md →</span>
          </a>
          <!-- QA -->
          <a
            href="https://notebooklm.google.com/notebook/ab916e69-f78b-47c3-9982-a5210a07d713"
            target="_blank"
            rel="noopener noreferrer"
            class="group flex flex-col items-center text-center gap-3 rounded-2xl border border-violet-200 bg-white p-6 shadow-sm hover:border-violet-400 hover:shadow-md transition-all"
          >
            <span class="text-3xl">🤖</span>
            <div>
              <p class="font-semibold text-arena-ink">Ask Questions</p>
              <p class="mt-1 text-xs text-arena-muted">Get instant answers with NotebookLM Live QA</p>
            </div>
            <span class="text-xs text-violet-600 group-hover:text-violet-800 font-medium mt-auto">Open NotebookLM →</span>
          </a>
          <!-- Compete -->
          <a
            href="/competitions"
            class="group flex flex-col items-center text-center gap-3 rounded-2xl border border-orange-200 bg-white p-6 shadow-sm hover:border-orange-400 hover:shadow-md transition-all"
          >
            <span class="text-3xl">🏁</span>
            <div>
              <p class="font-semibold text-arena-ink">Compete</p>
              <p class="mt-1 text-xs text-arena-muted">Join an active competition and race for the top</p>
            </div>
            <span class="text-xs text-orange-600 group-hover:text-orange-800 font-medium mt-auto">View Competitions →</span>
          </a>
        </div>
      </div>
    </div>


  </main>
  <Footer {...landing.footer} />
</Layout>

<script>
  document.querySelectorAll<HTMLButtonElement>('.copy-btn').forEach((btn) => {
    btn.addEventListener('click', async () => {
      const code = btn.dataset.code ?? '';
      await navigator.clipboard.writeText(code);

      const copyIcon = btn.querySelector('.copy-icon') as HTMLElement;
      const checkIcon = btn.querySelector('.check-icon') as HTMLElement;
      const label = btn.querySelector('.copy-label') as HTMLElement;

      copyIcon.classList.add('hidden');
      checkIcon.classList.remove('hidden');
      label.textContent = 'Copied!';

      setTimeout(() => {
        copyIcon.classList.remove('hidden');
        checkIcon.classList.add('hidden');
        label.textContent = 'Copy';
      }, 2000);
    });
  });
</script>
