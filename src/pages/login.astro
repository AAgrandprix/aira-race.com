---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import LoginManager from '../components/LoginManager.jsx';
import landing from '../data/landing.json';

const pageTitle = "Login | aira";
const pageDescription = "Login to your aira account to start competing.";
---

<Layout
  title={pageTitle}
  description={pageDescription}
  canonicalUrl={landing.site.url + '/login'}
  ogImage={landing.site.ogImage}
  siteName={landing.site.name}
  ogTitle={pageTitle}
  ogDescription={pageDescription}
  lang={landing.site.lang}
>
  <Header />
  <main class="flex-grow">
    <div class="min-h-full flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
      <div class="max-w-md w-full space-y-8 p-10 bg-slate-900 rounded-xl border border-slate-700">
        <div>
          <h2 class="mt-6 text-center text-3xl font-extrabold text-white">
            Log in to aira
          </h2>
          <p class="mt-2 text-center text-sm text-slate-400">
            to start your first AI race
          </p>
        </div>
        
        <LoginManager client:load />

      </div>
    </div>
  </main>
  <Footer {...landing.footer} />
</Layout>
