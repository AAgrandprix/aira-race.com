---
interface FooterLink {
  text: string;
  href: string;
}

interface Props {
  copyright: string;
  links: FooterLink[];
}

const { copyright, links } = Astro.props;
---

<footer class="py-8 border-t border-arena-border">
  <div class="max-w-5xl mx-auto px-4 sm:px-6 flex flex-col sm:flex-row items-center justify-between gap-4">
    <p class="text-sm text-arena-muted">{copyright}</p>
    <nav class="flex items-center gap-6">
      {links.map((link) => (
        <a
          href={link.href}
          target="_blank"
          rel="noopener noreferrer"
          class="text-sm text-arena-muted hover:text-arena-ink transition-colors"
        >
          {link.text}
        </a>
      ))}
    </nav>
  </div>
</footer>
